/**
 * Data table — tablecn (shadcn) components over TanStack Table, plus local
 * add-ons (topper, saved views, export, summary footer, row actions).
 *
 * @example
 * ```tsx
 * import { DataTable, DataTableToolbar, useDataTable } from "@caffeinebounce/ui/data-table";
 * ```
 * @module data-table
 */
export * from "../blocks/table";
