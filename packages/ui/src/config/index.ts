/**
 * Theme Configuration Exports
 *
 * Re-exports all theme configuration for easy imports.
 */

export {
  type Animation,
  animation,
  type BorderRadius,
  type Breakpoints,
  borderRadius,
  breakpoints,
  type Layout,
  layout,
  type Shadows,
  type Spacing,
  shadows,
  spacing,
  type Theme,
  type Typography,
  theme,
  typography,
  type ZIndex,
  zIndex,
} from "./theme";
