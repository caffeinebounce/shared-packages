/**
 * iCalendar (.ics) generation. Import from `@caffeinebounce/scheduling/ics`.
 */

export type { IcsAttendee, IcsEvent, IcsResult } from "./build";
export { buildIcs, formatIcsUtc } from "./build";
