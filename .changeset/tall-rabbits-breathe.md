---
"@caffeinebounce/identity": patch
---

Fix the published `@caffeinebounce/identity` package contract so both the root and `/server` entry points ship matching build outputs and declaration files.
